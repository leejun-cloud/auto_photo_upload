# StockFlow OS — 운영 매뉴얼

스톡 사진·영상을 여러 마켓플레이스(Adobe Stock·Shutterstock·Alamy 등)에 반자동으로 대량 업로드하는 도구. AI 메타데이터를 파일에 IPTC/XMP로 임베드하고 다중 (S)FTP로 병렬 전송한다.

## 설치
```bash
npm install
```
- `exiftool-vendored`가 exiftool 바이너리를 번들하므로 별도 시스템 설치 불필요.

## 실행
```bash
npm run dev      # 로컬 개발 (http://localhost:3000)
npm run build    # 프로덕션 빌드
npm start        # 빌드 결과 구동
npm test         # vitest
npx tsc --noEmit # 타입 체크
```

## 필수 환경변수
| 변수 | 용도 | 비고 |
|---|---|---|
| `CREDENTIALS_KEY` | 에이전시 FTP 비밀번호 AES-256-GCM 암호화 키 | **필수.** 32-byte base64. 생성: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` |
| `NEXT_PUBLIC_FIREBASE_*` (client) | 로그인용 Firebase 웹 설정 | **필수.** ⚠️ 아래 서비스계정과 **동일한 Firebase 프로젝트**여야 함(불일치 시 로그인 전부 401) |
| `FIREBASE_SERVICE_ACCOUNT_B64` | 서버 Firebase Admin 서비스계정(base64 JSON) | **필수.** client 설정과 같은 프로젝트여야 함 |
| `GEMINI_API_KEY` | Gemini vision 메타데이터 생성 | **필수.** 기본 모델 `gemini-2.5-flash` |
| `OPENROUTER_API_KEY` | 텍스트 정제(DeepSeek) | 선택(없으면 Gemini 텍스트로 폴백) |
| `AI_VISION_MODEL` / `AI_TEXT_MODEL` | AI 모델 오버라이드 | 선택. 기본 `gemini-2.5-flash` / `deepseek/deepseek-chat` |
| `CRON_SECRET` | 잡 워커 cron 인증 시크릿 | 프로덕션 필수(Vercel Cron `Authorization: Bearer <값>`) |
| `MAX_UPLOAD_BYTES` | 업로드 파일 크기 상한 | 선택. 기본 200MB |
| `DATABASE_URL` | Postgres 연결 문자열 | 있으면 Postgres, 없으면 로컬 SQLite. **Vercel 배포 시 필수**(파일시스템 휘발성) |
| `PGSSLMODE` | Postgres SSL 모드 | 선택 |
| `STORAGE_BACKEND` | `s3` 지정 시 S3 사용 | **Vercel 배포 시 `s3` 필수**(로컬 파일 휘발성) |
| `S3_BUCKET` / `S3_REGION` / `S3_ENDPOINT` / `S3_ACCESS_KEY_ID` / `S3_SECRET_ACCESS_KEY` / `S3_FORCE_PATH_STYLE` | S3 스토리지 설정 | MinIO 호환 |
| `COOKIE_SECURE` | 세션 쿠키 secure 플래그 | Vercel에서는 자동 |

> ⚠️ **로그인 전제조건**: `NEXT_PUBLIC_FIREBASE_*`(클라이언트)와 `FIREBASE_SERVICE_ACCOUNT_B64`(서버)가 **같은 Firebase 프로젝트**를 가리켜야 한다. 다르면 `verifySessionCookie`가 aud 불일치로 실패해 **모든 로그인이 401**이 된다.

## 현재 배포 상태 (LIVE)
- **프로덕션 URL: https://autophotoupload.vercel.app** (Vercel, 프로젝트 `auto_photo_upload`).
- Firebase 정본 프로젝트: **`auto-hub-2026-unique`** (client 웹설정 + 서버 서비스계정 동일. 이메일/비번 로그인 활성).
- **데이터 백엔드: Firestore** (`DB_BACKEND=firestore`) — 공유 프로젝트라 이 앱 데이터는 **`stockflow_` 접두어 컬렉션**에만 저장(다른 앱 데이터와 격리).
- **미디어 저장: Firebase Cloud Storage** (`STORAGE_BACKEND=firebase`) — 객체는 `stockflow/uploads/`, `stockflow/exports/` 경로에만 저장.
- 프로덕션 env: Firebase(client+admin), `GEMINI_API_KEY`, `OPENROUTER_API_KEY`, `CREDENTIALS_KEY`, `CRON_SECRET`, `DB_BACKEND=firestore`, `STORAGE_BACKEND=firebase`, `FIREBASE_STORAGE_BUCKET`.
- 라이브 검증 완료: 로그인 → 업로드(Firestore/Storage 영구 저장) → AI 메타데이터 → 배치 파이프라인 → cron 워커(`GET /api/jobs/tick` bearer 인증) 전 구간 200.
- 로컬 개발은 여전히 SQLite 기본값(별도 env 없이). `DB_BACKEND=firestore STORAGE_BACKEND=firebase`를 주면 로컬에서도 프로덕션과 동일 백엔드 사용.

> Firestore 컬렉션은 단일필드 인덱스만 사용(복합 인덱스 불필요). 잡 클레임은 `runTransaction`으로 원자적 처리.

## 배포 (Vercel)
1. `DATABASE_URL`(Postgres, 예: Neon) + S3 자격증명 + `CREDENTIALS_KEY`를 Vercel 프로젝트 환경변수에 등록.
2. DB 스키마 적용: `db/postgres-schema.sql`을 대상 Postgres에 1회 실행.
3. `git push` → Vercel 자동 배포.

> ⚠️ 로컬 기본값(SQLite + 파일시스템)은 Vercel 서버리스에서 데이터가 휘발된다. 프로덕션은 반드시 Postgres + S3.

## 간편 자동 업로드(배치) + 잡 큐
- 대시보드 "간편 자동 업로드": 사진 여러 장 선택 → 플랫폼 체크 → 버튼 1회로 업로드→AI 메타데이터→임베드→(S)FTP까지 자동 실행.
- 내부 동작: 각 파일이 `asset_pipeline` 잡으로 큐(`jobs` 테이블)에 등록되고 워커가 순차 처리. 실패 시 지수 백오프로 자동 재시도(최대 3회), "처리 현황"에서 수동 "다시 시도"도 가능.
- **잡 워커 구동 방법**:
  - 로컬: 클라이언트가 `POST /api/jobs/tick`로 폴링 구동(배치 실행 중 자동).
  - 프로덕션(Vercel): `vercel.json`의 cron이 2분마다 `GET /api/jobs/tick` 호출(`Authorization: Bearer $CRON_SECRET`). `CRON_SECRET` 미설정 시 cron 미작동.
- 잡 상태 조회: `GET /api/jobs`(사용자별). 상태 문구는 `src/lib/jobs/status-copy.ts`에서 한국어로 매핑.

## 자동화 범위 (합법 경계)
- **완전 자동**: AI 메타데이터 → IPTC/XMP 임베드 → 다중 (S)FTP 병렬 업로드.
- **반자동(HITL)**: 온보딩 폼 자동채움 → CAPTCHA/2FA/인증은 사용자가 처리.
- **사람 필수**: 계정 생성·W-8BEN·신분증·최종 Submit 클릭(에이전시 웹 UI).
- 1인 1계정 준수. CAPTCHA 솔버·가상번호·자동 다중계정 생성 금지(ToS 위반).

## 주요 모듈
- `src/lib/metadata-embed.ts` — IPTC/XMP 임베드(사진·영상)
- `src/lib/ftp/uploader.ts` — 다중 (S)FTP 팬아웃 + 재시도
- `src/lib/ftp/endpoints.ts` — 에이전시 FTP 엔드포인트
- `src/lib/crypto.ts` — 자격증명 암호화
- `src/lib/tax/w8ben.ts` — 한국 거주자 W-8BEN 자동 프리필(Article 14, 10%)
- `src/lib/jobs/pipeline.ts` — 자산 파이프라인(메타→임베드→FTP) 재사용 함수
- `src/lib/jobs/worker.ts` — 잡 클레임/처리/재시도 워커
- `src/lib/ai/metadata.ts` — Gemini vision + DeepSeek 텍스트 메타데이터 생성
- `src/lib/rate-limit.ts` / `src/lib/logger.ts` — 업로드 레이트리밋 / 구조화 로깅
- `db/schema.sql` / `db/postgres-schema.sql` — 스키마(`jobs` 테이블 포함)
- 리서치 근거: `docs/research-blueprint-2026.md`

## 자주 쓰는 운영 팁
- 실제 업로드는 각 에이전시 실계정 + FTP 자격증명 등록 후 동작. 대시보드 "FTP Credentials"에서 등록.
- 새 에이전시 추가: `src/lib/ftp/endpoints.ts`에 엔드포인트 + `adapters/index.ts`에 메타데이터 규칙 추가.
